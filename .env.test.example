# Docker 환경의 로컬 Supabase 전용. 실제 값은 .env.test.local에 입력한다.
# CI는 supabase status 출력에서 값을 가져오므로 GitHub Secrets가 필요 없다.
SUPABASE_URL=http://127.0.0.1:54321
SUPABASE_ANON_KEY=your-local-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-local-service-role-key-here
